appname: gml2json

# create table geojson
# (
#   smuserid NUMBER,
#   id       NUMBER,
#   area     NUMBER(5,2),
#   refname  NUMBER,
#   node     NUMBER,
#   tag      NUMBER,
#   geometry CLOB
# )

db:
    user:     username
    password: password
    tnsname:  tnsname

redis:
    network: tcp
    address: "127.0.0.1:6379"  
    maxidle: 2
    maxactive: 10000
    idletimeout: 300
    database: 0
